import { Title } from '../header-feature/components/Title'

export const Header = () => {
    return (
        <header id="main-header">
            <Title>ReactFood</Title>
        </header>
    );
    }